image: node:18.17.0

pipelines:
  pull-requests:
    hotfix/*:
      - step:
          name: Install
          caches:
            - nodecustom
            - yarn
          script:
            - unset CI
            - yarn set version 4.1.1
            - yarn
          artifacts:
            - node_modules/**
      - step:
          name: Build and push tot container registry
          deployment: open-acceptatie-tilburg-nl
          script:
            - unset CI
            - env | awk -F"DOTENV_" '/^DOTENV_/ {print $2}' >> .env
            - yarn set version 4.1.1
            - yarn build:web
            - docker login $DOCKER_DOMAIN -u $DOCKER_HUB_USER -p $DOCKER_HUB_PASSWORD
            - COMMIT_HASH=$BITBUCKET_COMMIT
            - docker build -t $DOCKER_DOMAIN/tilburg-woo-staging:latest .
            - docker push $DOCKER_DOMAIN/tilburg-woo-ui-staging:latest
          services:
            - docker
  branches:
    develop:
      - step:
          name: Install
          caches:
            - nodecustom
            - yarn
          script:
            - unset CI
            - yarn set version 4.1.1
            - yarn
          artifacts:
            - node_modules/**
      - step:
          name: Build and push tot container registry
          deployment: develop
          script:
            - unset CI
            - env | awk -F"DOTENV_" '/^DOTENV_/ {print $2}' >> .env
            - yarn set version 4.1.1
            - yarn build:web
            - docker login $DOCKER_DOMAIN -u $DOCKER_HUB_USER -p $DOCKER_HUB_PASSWORD
            - COMMIT_HASH=$BITBUCKET_COMMIT
            - docker build -t $DOCKER_DOMAIN/tilburg-woo-ui-develop:latest .
            - docker push $DOCKER_DOMAIN/tilburg-woo-ui-develop:latest
          services:
            - docker
    release/*:
      - step:
          name: Install
          caches:
            - nodecustom
            - yarn
          script:
            - unset CI
            - yarn set version 4.1.1
            - yarn
          artifacts:
            - node_modules/**
      - step:
          name: Build and push tot container registry
          deployment: open-acceptatie-tilburg-nl
          script:
            - unset CI
            - env | awk -F"DOTENV_" '/^DOTENV_/ {print $2}' >> .env
            - yarn set version 4.1.1
            - yarn build:web
            - docker login $DOCKER_DOMAIN -u $DOCKER_HUB_USER -p $DOCKER_HUB_PASSWORD
            - COMMIT_HASH=$BITBUCKET_COMMIT
            - docker build -t $DOCKER_DOMAIN/tilburg-woo-ui-staging:latest .
            - docker push $DOCKER_DOMAIN/tilburg-woo-ui-staging:latest
          services:
            - docker
  custom: # Pipelines that are triggered manually
    conduction:
      - step:
          name: Install
          caches:
            - nodecustom
            - yarn
          script:
            - unset CI
            - yarn set version 4.1.1
            - yarn
          artifacts:
            - node_modules/**
      - step:
          name: Build and push tot container registry
          deployment: conduction
          script:
            - unset CI
            - env | awk -F"DOTENV_" '/^DOTENV_/ {print $2}' >> .env
            - yarn set version 4.1.1
            - yarn build:web
            - echo 'Set FE API url to "$API_URL_COMMONGROUND"'
            - docker login -u $DOCKER_HUB_USER -p $DOCKER_HUB_PASSWORD
            - COMMIT_HASH=$BITBUCKET_COMMIT
            - docker build -t acatonl/woo-ui-develop:latest .
            - docker push acatonl/woo-ui-develop:latest
          services:
            - docker
    production:
      - step:
          name: Install
          caches:
            - nodecustom
            - yarn
          script:
            - unset CI
            - yarn set version 4.1.1
            - yarn
          artifacts:
            - node_modules/**
      - step:
          name: Build and push tot container registry
          deployment: production
          script:
            - unset CI
            - env | awk -F"DOTENV_" '/^DOTENV_/ {print $2}' >> .env
            - yarn set version 4.1.1
            - yarn build:web
            - docker login $DOCKER_DOMAIN -u $DOCKER_HUB_USER -p $DOCKER_HUB_PASSWORD
            - SHORT_COMMIT_HASH=$(echo $BITBUCKET_COMMIT | cut -c 1-7)
            - docker build -t $DOCKER_DOMAIN/acato/tilburg-woo-ui:$SHORT_COMMIT_HASH .
            - docker push $DOCKER_DOMAIN/acato/tilburg-woo-ui:$SHORT_COMMIT_HASH
          services:
              - docker
definitions:
  caches:
    nodecustom: ./node_modules
    yarn: /usr/local/share/.cache/yarn