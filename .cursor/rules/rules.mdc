---
description: Infinite loops with the useEffect hook cannot be fixed by changing useEffect code or adding dependecies.
alwaysApply: false
---
you cant fix an infinite loop in a useEffect by adding more dependencies or changing the useEffect code.

You have to either reduce the dependencies OR fix the dependencies.
for example it might create a new object reference for the same data, causing infinite refreshes.